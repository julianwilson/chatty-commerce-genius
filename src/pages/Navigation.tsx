import React from 'react';
import { Navigation as NavigationComponent } from '@/components/navigation/Navigation';

const Navigation: React.FC = () => {
  return <NavigationComponent />;
};

export default Navigation;
